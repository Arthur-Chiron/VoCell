# Explorateur volumétrique (src/app.py) — installable sur n'importe quelle machine.
# Les dépendances du pipeline SAM3D (GPU) sont dans requirements-sam3d.txt.

streamlit>=1.49   # width="stretch" sur les boutons requiert >= 1.49
numpy
scipy
plotly
pyarrow           # requis par st.components.v1 (nuage de noyaux)

# scripts/preprocess_restore.py : lecture des fichiers Imaris (.ims = HDF5)
h5py

# scripts/build_cloud.py : encodage des atlas de vignettes du nuage
pillow

# Augmentations partagées avec cellf-supervised (ObliqueSection).
# Source unique : https://github.com/Arthur-Chiron/cellaug — ne pas recopier
# le code ici. En développement, préférer un clone local éditable commun aux
# deux projets, qui court-circuite cette ligne :
#   git clone https://github.com/Arthur-Chiron/cellaug.git ../cellaug
#   pip install -e ../cellaug
cellaug @ git+https://github.com/Arthur-Chiron/cellaug.git@main
